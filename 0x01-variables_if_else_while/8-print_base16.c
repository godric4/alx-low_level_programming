#include <stdio.h>
/**
 *  * main - entry point
 *   * display numbers
 *    * Return: 0 success
 *     */
int main(void)
{
char hex
for (hex = 48; hex <= 57; hex++)
{
putchar(putchar(hex);
}
for (hex = 97; hex <= 102; hex++)
{
putchar(hex);
}
putchar('\n');

return (0);
}
