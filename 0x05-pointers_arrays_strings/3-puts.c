#include "main.h"

/**
 * _puts - prints a string to the standard output
 * @str: parameter that points to string to be printed
 */

void _puts(char *str)
{
int str_val;

str = 0;
while (str_val != '\0')
{
str_val++;
}
_putchar(str_val);
_putchar('\n');
}
